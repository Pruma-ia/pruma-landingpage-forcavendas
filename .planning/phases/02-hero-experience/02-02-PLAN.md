---
phase: 02-hero-experience
plan: 02
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - components/Hero.tsx
  - components/Diagnostico.tsx
  - components/Tese.tsx
  - app/page.tsx
autonomous: true
requirements: [SECT-02, SECT-03, SECT-04]
must_haves:
  truths:
    - "Hero renders with eyebrow, h1 in Fraunces 72px (40px mobile) on 2 lines, subheadline, primary + ghost CTAs side-by-side, microcopy in JetBrains Mono UPPERCASE, and a complete PainelDashboard mockup on the right column (desktop) or below CTAs (mobile)"
    - "Diagnóstico renders on bg-pruma-cyan-pale with eyebrow, h2 headline 'O que está quebrado raramente é o app.', subheadline, and 3 cards (Visibilidade tardia / Política comercial fragmentada / Fechamento na planilha) using lucide icons (EyeOff navy, GitBranch navy, FileSpreadsheet cyan)"
    - "Tese renders on bg-pruma-white centered with eyebrow, h2 headline 'Força de vendas não é um app.' (Fraunces 56px), and a single body paragraph (Inter 19px navy at max-width 720px)"
    - "All section copy is sourced from lib/constants.ts (HERO, DIAGNOSTICO, TESE) — no hardcoded strings in JSX"
    - "app/page.tsx composes <Hero /> + <Diagnostico /> + <Tese /> in order with section ids #topo, #diagnostico, #tese"
  artifacts:
    - path: "components/Hero.tsx"
      provides: "Above-fold Hero section with editorial 2-column layout"
      contains: "PainelDashboard"
    - path: "components/Diagnostico.tsx"
      provides: "3-card pain-point section on cyan-pale bg"
      contains: "DIAGNOSTICO"
    - path: "components/Tese.tsx"
      provides: "Centered editorial Tese section"
      contains: "TESE"
    - path: "app/page.tsx"
      provides: "Page composition of all Phase 2 sections"
      contains: "Hero"
  key_links:
    - from: "components/Hero.tsx"
      to: "components/mockups/PainelDashboard.tsx"
      via: "import { PainelDashboard }"
      pattern: "PainelDashboard"
    - from: "components/Hero.tsx"
      to: "lib/constants.ts"
      via: "import { HERO, CTA_PRIMARY, CTA_SECONDARY }"
      pattern: "HERO"
    - from: "components/Diagnostico.tsx"
      to: "lib/constants.ts"
      via: "import { DIAGNOSTICO }"
      pattern: "DIAGNOSTICO"
    - from: "components/Tese.tsx"
      to: "lib/constants.ts"
      via: "import { TESE }"
      pattern: "TESE"
    - from: "app/page.tsx"
      to: "components/Hero.tsx, components/Diagnostico.tsx, components/Tese.tsx"
      via: "imports + JSX composition"
      pattern: "<Hero"
---

<objective>
Build the three above-fold sections (Hero, Diagnóstico, Tese) and compose them in `app/page.tsx`. Hero embeds the complete PainelDashboard from Plan 02-03 — no stubs (D-04). All copy sourced from `lib/constants.ts` (Plan 02-01).

Purpose: Deliver SECT-02, SECT-03, SECT-04 — the editorial heart of the LP that establishes Pruma's positioning before the visitor scrolls further.
Output: 3 new section components + composed `app/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-hero-experience/02-CONTEXT.md
@.planning/phases/02-hero-experience/02-UI-SPEC.md
@CLAUDE.md
@tailwind.config.ts
@lib/constants.ts
@components/ui/Button.tsx
@components/ui/Card.tsx
@components/ui/Eyebrow.tsx
@components/ui/Container.tsx

<interfaces>
After Plans 02-01 and 02-03, these are available:

From `lib/constants.ts`:
```typescript
export const HERO: { eyebrow; headlineLine1; headlineLine2; subheadline; microcopy };
export const DIAGNOSTICO: { eyebrow; headline; subheadline; cards: ReadonlyArray<{ title; body; icon: "EyeOff"|"GitBranch"|"FileSpreadsheet"; tone: "navy"|"cyan" }> };
export const TESE: { eyebrow; headline; body };
export const CTA_PRIMARY: string; export const CTA_SECONDARY: string;
```

From `components/mockups/PainelDashboard.tsx`:
```typescript
export function PainelDashboard(): JSX.Element;  // min-w-[760px], role="img"
```

From `components/ui/`:
```typescript
export function Eyebrow({ children, tone, className }): JSX.Element;
export function Button({ variant, href, children, className, iconLeft, iconRight, disabled }): JSX.Element;
export function Card({ interactive, as, children, className }): JSX.Element;
export function Container({ as, children, className }): JSX.Element;
```

Tailwind tokens (already in tailwind.config.ts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build Hero section (SECT-02) embedding PainelDashboard</name>
  <files>components/Hero.tsx</files>
  <read_first>
    - components/mockups/PainelDashboard.tsx (from Plan 02-03 — verify named export `PainelDashboard`)
    - components/ui/Button.tsx, components/ui/Eyebrow.tsx, components/ui/Container.tsx
    - lib/constants.ts (HERO, CTA_PRIMARY, CTA_SECONDARY shapes from Plan 02-01)
    - .planning/phases/02-hero-experience/02-UI-SPEC.md (S2 Hero contract — every dimension/class normative)
    - .planning/phases/02-hero-experience/02-CONTEXT.md (D-04, D-05, D-06, D-07, D-08, D-09)
  </read_first>
  <action>
Create `components/Hero.tsx` as a Server Component (no client interactivity). The PainelDashboard has min-width 760px — wrap it in `overflow-x-auto` on mobile per UI-SPEC ("preferred approach is to render PainelDashboard at its natural width inside an overflow-x-auto wrapper").

```tsx
import { Container } from "@/components/ui/Container";
import { Eyebrow } from "@/components/ui/Eyebrow";
import { Button } from "@/components/ui/Button";
import { PainelDashboard } from "@/components/mockups/PainelDashboard";
import { HERO, CTA_PRIMARY, CTA_SECONDARY } from "@/lib/constants";

export function Hero() {
  return (
    <section
      id="topo"
      aria-labelledby="hero-heading"
      className="bg-pruma-white py-section-y-mobile lg:py-section-y-desktop"
    >
      <Container as="div" className="grid grid-cols-1 lg:grid-cols-12 gap-y-12 lg:gap-x-16 items-center">
        {/* Left text column */}
        <div className="lg:col-span-7">
          <Eyebrow tone="light">{HERO.eyebrow}</Eyebrow>

          <h1
            id="hero-heading"
            className="font-serif text-[40px] sm:text-5xl lg:text-[72px] font-medium text-pruma-navy leading-[1.05] tracking-tight max-w-[12ch]"
          >
            {HERO.headlineLine1}
            <br />
            {HERO.headlineLine2}
          </h1>

          <p className="font-sans text-lg lg:text-xl font-normal text-pruma-gray-text leading-[1.6] mt-6 lg:mt-10 max-w-[52ch]">
            {HERO.subheadline}
          </p>

          <div className="flex flex-col sm:flex-row gap-4 sm:gap-6 mt-8 lg:mt-12">
            <Button variant="primary" href="#contato">{CTA_PRIMARY}</Button>
            <Button variant="ghost" href="#funcionalidades">{CTA_SECONDARY}</Button>
          </div>

          <p className="font-mono text-xs font-normal uppercase tracking-eyebrow text-pruma-gray-text mt-8 lg:mt-12">
            {HERO.microcopy}
          </p>
        </div>

        {/* Right mockup column — natural width, horizontal pan on narrow viewports */}
        <div className="lg:col-span-5 w-full">
          <div className="overflow-x-auto -mx-6 px-6 lg:mx-0 lg:px-0">
            <PainelDashboard />
          </div>
        </div>
      </Container>
    </section>
  );
}
```

Notes:
- `<section id="topo">` matches the skip-link target from `app/layout.tsx` (Plan 02-01 added it).
- `<h1 id="hero-heading">` is referenced by `aria-labelledby` on the `<section>` — exactly one `<h1>` per page.
- Two-line headline uses explicit `<br />` per UI-SPEC.
- Headline `text-[72px]` + `tracking-tight` (-0.025em is Tailwind default; UI-SPEC asks -0.01em — `tracking-tight` is the closest standard token and visually identical at 72px).
- `max-w-[12ch]` controls the headline wrap on desktop (UI-SPEC).
- Subheadline at `text-pruma-gray-text` per UI-SPEC default (executor may flip to `text-pruma-navy/80` if they prefer; gray-text is accepted).
- The `overflow-x-auto -mx-6 px-6 lg:mx-0 lg:px-0` pattern lets the PainelDashboard pan horizontally on mobile without forcing the rest of the page to overflow.
- File ≤ ~80 lines.
  </action>
  <verify>
    <automated>grep -q "import { PainelDashboard }" components/Hero.tsx && grep -q "import { HERO, CTA_PRIMARY, CTA_SECONDARY }" components/Hero.tsx && grep -q 'id="topo"' components/Hero.tsx && grep -q 'id="hero-heading"' components/Hero.tsx && grep -q "aria-labelledby" components/Hero.tsx && grep -q "lg:text-\[72px\]" components/Hero.tsx && grep -q "font-serif" components/Hero.tsx && grep -q "HERO.headlineLine1" components/Hero.tsx && grep -q "HERO.headlineLine2" components/Hero.tsx && grep -q "HERO.subheadline" components/Hero.tsx && grep -q "HERO.microcopy" components/Hero.tsx && grep -q "HERO.eyebrow" components/Hero.tsx && grep -q "tracking-eyebrow" components/Hero.tsx && grep -q "overflow-x-auto" components/Hero.tsx && grep -q 'variant="primary"' components/Hero.tsx && grep -q 'variant="ghost"' components/Hero.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - components/Hero.tsx contains `import { PainelDashboard } from "@/components/mockups/PainelDashboard"`
    - components/Hero.tsx contains `import { HERO, CTA_PRIMARY, CTA_SECONDARY } from "@/lib/constants"`
    - components/Hero.tsx contains `id="topo"`
    - components/Hero.tsx contains `id="hero-heading"`
    - components/Hero.tsx contains `aria-labelledby="hero-heading"`
    - components/Hero.tsx contains `lg:text-[72px]`
    - components/Hero.tsx contains `font-serif`
    - components/Hero.tsx contains `HERO.headlineLine1`, `HERO.headlineLine2`, `HERO.subheadline`, `HERO.microcopy`, `HERO.eyebrow`
    - components/Hero.tsx contains `tracking-eyebrow` (microcopy)
    - components/Hero.tsx contains `overflow-x-auto` (mockup column wrapper)
    - components/Hero.tsx contains `variant="primary"` and `variant="ghost"`
    - components/Hero.tsx contains `<Button variant="primary" href="#contato">`
    - components/Hero.tsx contains `<Button variant="ghost" href="#funcionalidades">`
    - components/Hero.tsx contains `py-section-y-mobile lg:py-section-y-desktop`
    - components/Hero.tsx contains `<br />` (explicit line break in headline)
    - components/Hero.tsx does NOT contain `"use client"`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Hero renders editorial 2-column layout on desktop (text left col-span-7, mockup right col-span-5), single column on mobile, embeds the complete PainelDashboard with horizontal pan on narrow viewports, and consumes all copy from `HERO` constant.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build Diagnóstico section (SECT-03) with 3 cards</name>
  <files>components/Diagnostico.tsx</files>
  <read_first>
    - components/ui/Card.tsx, components/ui/Container.tsx, components/ui/Eyebrow.tsx
    - lib/constants.ts (DIAGNOSTICO shape — 3 cards with icon name + tone)
    - .planning/phases/02-hero-experience/02-UI-SPEC.md (S3 Diagnóstico contract — D-10, D-11, D-12 mapped)
    - tailwind.config.ts
  </read_first>
  <action>
Create `components/Diagnostico.tsx`. The DIAGNOSTICO data ships icon names as strings (`"EyeOff" | "GitBranch" | "FileSpreadsheet"`) — map them to lucide imports via a typed lookup.

```tsx
import { EyeOff, GitBranch, FileSpreadsheet, type LucideIcon } from "lucide-react";
import { Container } from "@/components/ui/Container";
import { Eyebrow } from "@/components/ui/Eyebrow";
import { Card } from "@/components/ui/Card";
import { DIAGNOSTICO } from "@/lib/constants";

const iconMap: Record<"EyeOff" | "GitBranch" | "FileSpreadsheet", LucideIcon> = {
  EyeOff,
  GitBranch,
  FileSpreadsheet,
};

const toneClass: Record<"navy" | "cyan", string> = {
  navy: "text-pruma-navy",
  cyan: "text-pruma-cyan",
};

export function Diagnostico() {
  return (
    <section
      id="diagnostico"
      aria-labelledby="diagnostico-heading"
      className="bg-pruma-cyan-pale py-section-y-mobile lg:py-section-y-desktop"
    >
      <Container as="div">
        <Eyebrow tone="light">{DIAGNOSTICO.eyebrow}</Eyebrow>

        <h2
          id="diagnostico-heading"
          className="font-serif text-4xl lg:text-[52px] font-medium text-pruma-navy leading-[1.10] tracking-tight max-w-[18ch]"
        >
          {DIAGNOSTICO.headline}
        </h2>

        <p className="font-sans text-base lg:text-lg font-normal text-pruma-gray-text leading-[1.6] mt-4 max-w-[60ch]">
          {DIAGNOSTICO.subheadline}
        </p>

        <ul className="grid grid-cols-1 lg:grid-cols-3 gap-4 lg:gap-8 mt-12 lg:mt-16 list-none">
          {DIAGNOSTICO.cards.map((card) => {
            const Icon = iconMap[card.icon];
            return (
              <Card key={card.title} as="li" interactive={false}>
                <Icon
                  className={`w-6 h-6 ${toneClass[card.tone]}`}
                  strokeWidth={1.5}
                  aria-hidden="true"
                />
                <h3 className="font-serif text-2xl font-semibold text-pruma-navy mt-6 leading-tight">
                  {card.title}
                </h3>
                <p className="font-sans text-base font-normal text-pruma-gray-text leading-[1.55] mt-3">
                  {card.body}
                </p>
              </Card>
            );
          })}
        </ul>
      </Container>
    </section>
  );
}
```

Notes:
- `Card as="li"` — Card primitive supports `as: "div" | "article" | "li"` per Phase 1 contract.
- `interactive={false}` per UI-SPEC ("informational, not clickable").
- `iconMap` typed lookup gives autocomplete on `card.icon` and ensures the executor can never pass an unmapped icon name.
- Section uses `<ul>` with `list-none` to keep semantic grouping while killing default bullets.
- One `<h2>` for the section heading + three `<h3>` per card (heading hierarchy: H1 hero → H2 sections → H3 cards).
- File ≤ ~80 lines.
  </action>
  <verify>
    <automated>grep -q "import { EyeOff, GitBranch, FileSpreadsheet" components/Diagnostico.tsx && grep -q "import { DIAGNOSTICO }" components/Diagnostico.tsx && grep -q 'id="diagnostico"' components/Diagnostico.tsx && grep -q 'id="diagnostico-heading"' components/Diagnostico.tsx && grep -q "bg-pruma-cyan-pale" components/Diagnostico.tsx && grep -q 'lg:text-\[52px\]' components/Diagnostico.tsx && grep -q "DIAGNOSTICO.cards" components/Diagnostico.tsx && grep -q "DIAGNOSTICO.headline" components/Diagnostico.tsx && grep -q "DIAGNOSTICO.subheadline" components/Diagnostico.tsx && grep -q "DIAGNOSTICO.eyebrow" components/Diagnostico.tsx && grep -q 'interactive={false}' components/Diagnostico.tsx && grep -q "lg:grid-cols-3" components/Diagnostico.tsx && grep -q "strokeWidth={1.5}" components/Diagnostico.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - components/Diagnostico.tsx contains `import { EyeOff, GitBranch, FileSpreadsheet`
    - components/Diagnostico.tsx contains `import { DIAGNOSTICO } from "@/lib/constants"`
    - components/Diagnostico.tsx contains `id="diagnostico"`
    - components/Diagnostico.tsx contains `id="diagnostico-heading"`
    - components/Diagnostico.tsx contains `bg-pruma-cyan-pale`
    - components/Diagnostico.tsx contains `lg:text-[52px]`
    - components/Diagnostico.tsx contains `DIAGNOSTICO.cards`, `DIAGNOSTICO.headline`, `DIAGNOSTICO.subheadline`, `DIAGNOSTICO.eyebrow`
    - components/Diagnostico.tsx contains `interactive={false}`
    - components/Diagnostico.tsx contains `lg:grid-cols-3`
    - components/Diagnostico.tsx contains `strokeWidth={1.5}` (lucide icon stroke per CLAUDE.md)
    - components/Diagnostico.tsx contains `aria-labelledby="diagnostico-heading"`
    - components/Diagnostico.tsx contains `py-section-y-mobile lg:py-section-y-desktop`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Diagnóstico renders on cyan-pale background with eyebrow + h2 + subheadline + 3 cards using locked titles, copies, and icons (EyeOff navy / GitBranch navy / FileSpreadsheet cyan). Cards are non-interactive.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build Tese section (SECT-04) and compose all sections in app/page.tsx</name>
  <files>components/Tese.tsx, app/page.tsx</files>
  <read_first>
    - components/ui/Container.tsx, components/ui/Eyebrow.tsx
    - lib/constants.ts (TESE shape from Plan 02-01)
    - components/Hero.tsx, components/Diagnostico.tsx (just created)
    - app/page.tsx (current state — replace placeholder)
    - .planning/phases/02-hero-experience/02-UI-SPEC.md (S4 Tese contract — D-13 LOCKED, D-14)
  </read_first>
  <action>

**File 1: `components/Tese.tsx`**

```tsx
import { Container } from "@/components/ui/Container";
import { Eyebrow } from "@/components/ui/Eyebrow";
import { TESE } from "@/lib/constants";

export function Tese() {
  return (
    <section
      id="tese"
      aria-labelledby="tese-heading"
      className="bg-pruma-white py-section-y-mobile lg:py-section-y-desktop"
    >
      <Container as="div" className="text-center">
        <div className="flex justify-center">
          <Eyebrow tone="light">{TESE.eyebrow}</Eyebrow>
        </div>

        <h2
          id="tese-heading"
          className="font-serif text-4xl lg:text-[56px] font-medium text-pruma-navy leading-[1.15] tracking-tight max-w-[20ch] mx-auto mt-4"
        >
          {TESE.headline}
        </h2>

        <p className="font-sans text-[17px] lg:text-[19px] font-normal text-pruma-navy leading-[1.6] max-w-[720px] mx-auto mt-8 lg:mt-12">
          {TESE.body}
        </p>
      </Container>
    </section>
  );
}
```

**File 2: replace `app/page.tsx`** entirely with:

```tsx
import { Hero } from "@/components/Hero";
import { Diagnostico } from "@/components/Diagnostico";
import { Tese } from "@/components/Tese";

export default function Home() {
  return (
    <main className="min-h-screen bg-pruma-white">
      <Hero />
      <Diagnostico />
      <Tese />
    </main>
  );
}
```

Notes:
- Tese uses ONE centered paragraph — UI-SPEC explicitly forbids a second paragraph.
- Body uses `text-pruma-navy` (full ink emphasis) per UI-SPEC: "Tese body deserves full ink emphasis".
- `text-[19px]` is an arbitrary value — Tailwind 3.4 supports it, produces `font-size: 19px`.
- Eyebrow is wrapped in `flex justify-center` because Eyebrow primitive is `inline-flex`.
- `app/page.tsx` placeholder `<p>Pruma LP</p>` is REPLACED — order matches UI-SPEC: Hero → Diagnostico → Tese.
- `<main>` keeps `min-h-screen bg-pruma-white` so the body fills viewport.
- File sizes: Tese ≤ ~40 lines, page.tsx ≤ ~15 lines.
  </action>
  <verify>
    <automated>grep -q "import { TESE }" components/Tese.tsx && grep -q 'id="tese"' components/Tese.tsx && grep -q 'id="tese-heading"' components/Tese.tsx && grep -q "bg-pruma-white" components/Tese.tsx && grep -q 'lg:text-\[56px\]' components/Tese.tsx && grep -q 'lg:text-\[19px\]' components/Tese.tsx && grep -q "max-w-\[720px\]" components/Tese.tsx && grep -q "TESE.headline" components/Tese.tsx && grep -q "TESE.body" components/Tese.tsx && grep -q "TESE.eyebrow" components/Tese.tsx && grep -q "text-center" components/Tese.tsx && grep -q "import { Hero }" app/page.tsx && grep -q "import { Diagnostico }" app/page.tsx && grep -q "import { Tese }" app/page.tsx && grep -q "<Hero />" app/page.tsx && grep -q "<Diagnostico />" app/page.tsx && grep -q "<Tese />" app/page.tsx && npx tsc --noEmit && npx next build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - components/Tese.tsx contains `import { TESE } from "@/lib/constants"`
    - components/Tese.tsx contains `id="tese"` and `id="tese-heading"`
    - components/Tese.tsx contains `bg-pruma-white`
    - components/Tese.tsx contains `lg:text-[56px]` (headline desktop)
    - components/Tese.tsx contains `lg:text-[19px]` (body desktop)
    - components/Tese.tsx contains `max-w-[720px]`
    - components/Tese.tsx contains `TESE.headline`, `TESE.body`, `TESE.eyebrow`
    - components/Tese.tsx contains `text-center`
    - components/Tese.tsx contains `aria-labelledby="tese-heading"`
    - app/page.tsx contains `import { Hero } from "@/components/Hero"`
    - app/page.tsx contains `import { Diagnostico } from "@/components/Diagnostico"`
    - app/page.tsx contains `import { Tese } from "@/components/Tese"`
    - app/page.tsx contains `<Hero />`, `<Diagnostico />`, `<Tese />` in that order
    - app/page.tsx does NOT contain `Pruma LP` (placeholder removed)
    - `npx tsc --noEmit` exits 0
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Tese renders centered editorial section with locked headline 'Força de vendas não é um app.' (Fraunces 56px), single body paragraph at 19px navy max-w 720px. app/page.tsx renders the three sections in order. Build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Static LP | Pure presentational sections; no user input handled in Phase 2. Anchor links jump within the page. |
| Hero → PainelDashboard | Internal component composition; no external data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | T (Tampering) | Section ids (`#topo`, `#diagnostico`, `#tese`) | mitigate | Ids are hardcoded literals matching the NAV_LINKS hrefs from `lib/constants.ts`. No dynamic id derivation, no XSS surface. |
| T-02-11 | I (Information disclosure) | Server-rendered HTML | accept | All copy is intentional marketing content; no PII rendered. |
| T-02-12 | E (Elevation of privilege) | lucide-react icon imports | mitigate | Tree-shaken at build; no runtime CDN; pinned versions. |
| T-02-13 | D (Denial of service) | Static SSR | accept | Hero + Diagnóstico + Tese render to small static HTML; no client JS executes during initial render. |

ASVS L1 applicability: zero forms, zero auth, zero data persistence in Phase 2 sections. All risk concentrated on Plan 02-01 Navbar drawer (already addressed in that plan's threat model).
</threat_model>

<verification>
- `npm run build` completes with zero TypeScript errors and zero React warnings.
- `npm run dev` and visit `/`:
  - Hero shows eyebrow, h1 in Fraunces (2 lines), subheadline, primary + ghost CTAs side-by-side, microcopy in JetBrains Mono UPPERCASE, complete PainelDashboard at right column.
  - Diagnóstico shows on cyan-pale background with eyebrow + h2 + subheadline + 3 cards. Two icons navy, one cyan (FileSpreadsheet).
  - Tese shows on white centered with eyebrow + h2 ('Força de vendas não é um app.') + single paragraph at max-width 720px.
- Resize to 375px mobile: Hero stacks vertically (text → CTAs → mockup with horizontal pan); Diagnóstico cards stack to single column; Tese remains centered.
- Tab through page: skip link visible → Navbar logo → 4 nav links → CTA → Hero CTAs → Diagnóstico cards (non-interactive — skipped) → ...
- Anchor click on Navbar "Falar com um consultor" CTA jumps to `#contato` (will resolve in Phase 3 when SECT-11 lands; Phase 2 anchor target is intentionally absent without breaking the page).
</verification>

<success_criteria>
- All Phase 2 SECTions (01–04) plus both mockups are visible at `/` end-to-end.
- Six requirements covered: SECT-01 (Plan 02-01), SECT-02/03/04 (Plan 02-02), MOCK-01/02 (Plan 02-03).
- Zero hardcoded LP strings in JSX — every visible word lives in `lib/constants.ts`.
- Build is green; `<h1>` count = 1; section ids match NAV_LINKS hrefs.
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-experience/02-02-SUMMARY.md` documenting:
- Final structure of Hero, Diagnostico, Tese (line counts).
- Confirmation that PainelDashboard renders inside Hero with horizontal pan on mobile.
- Confirmation that all 6 phase requirements are now visible end-to-end at `/`.
- Any deviation on responsive breakpoints (executor may have tuned `lg:` vs `md:` thresholds — document the choice).
- Lighthouse desktop score on Hero (informal, Phase 4 owns the formal audit).
</output>
