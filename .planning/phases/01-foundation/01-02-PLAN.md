---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - app/layout.tsx
  - lib/constants.ts
  - components/ui/Eyebrow.tsx
  - components/ui/Button.tsx
  - components/ui/Card.tsx
  - components/ui/Container.tsx
autonomous: true
requirements:
  - SETUP-05
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "SEO metadata declara title exato e Open Graph tags no `<head>` da página"
    - "`<Eyebrow>` renderiza linha horizontal ciano 32px + texto JetBrains Mono UPPERCASE 12px com tracking 0.12em"
    - "`<Button variant='primary'>` renderiza com fundo `#0D1B4B`, radius 8px, e aplica `translateY(-1px)` + glow ciano no hover"
    - "`<Button variant='ghost'>` renderiza fundo transparente com borda `#0D1B4B` e preenche `#E0F6FE` no hover"
    - "`<Card>` renderiza fundo branco, borda `#F4F6F9`, radius 12px, e aplica `translateY(-2px)` + borda `#5CCFF5` no hover"
    - "`<Container>` limita o conteúdo a `max-w-6xl` (1152px) e não define background próprio"
    - "Todos os primitivos respeitam `prefers-reduced-motion: reduce`"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Metadata SEO completa com title, description, e openGraph"
      contains: "Pruma | App Web de Força de Vendas B2B para Indústrias e Distribuidoras"
    - path: "lib/constants.ts"
      provides: "Strings canônicas de CTA e metadados para consumo por componentes"
      exports: ["SITE_TITLE", "SITE_DESCRIPTION", "CTA_PRIMARY", "CTA_SECONDARY", "CTA_FORM"]
    - path: "components/ui/Eyebrow.tsx"
      provides: "Componente Eyebrow com props { children, tone?, className? }"
      exports: ["Eyebrow"]
    - path: "components/ui/Button.tsx"
      provides: "Componente Button com variants primary/ghost e suporte a href"
      exports: ["Button"]
    - path: "components/ui/Card.tsx"
      provides: "Componente Card com interactive prop e as prop"
      exports: ["Card"]
    - path: "components/ui/Container.tsx"
      provides: "Componente Container com max-w-6xl e as prop"
      exports: ["Container"]
  key_links:
    - from: "app/layout.tsx"
      to: "lib/constants.ts"
      via: "importação de SITE_TITLE e SITE_DESCRIPTION para metadata"
      pattern: "SITE_TITLE|SITE_DESCRIPTION"
    - from: "components/ui/Button.tsx"
      to: "tailwind.config.ts tokens"
      via: "classes pruma-navy, pruma-cyan, rounded-pruma-sm, shadow-pruma-cyan"
      pattern: "bg-pruma-navy|rounded-pruma-sm"
    - from: "components/ui/Card.tsx"
      to: "tailwind.config.ts tokens"
      via: "classes rounded-pruma-md, shadow-pruma-sm, border-pruma-gray-soft"
      pattern: "rounded-pruma-md|shadow-pruma-sm"
---

<objective>
Completar a fundação com metadados SEO corretos em `app/layout.tsx`, criar `lib/constants.ts` com as strings canônicas da LP, e implementar os quatro componentes UI primitivos: Eyebrow, Button, Card e Container.

Purpose: Estes primitivos são os blocos construtores de todas as 12 seções. Cada fase subsequente os consome — erros aqui se propagam para todo o projeto. Devem ser implementados com fidelidade exata à UI-SPEC.
Output: Quatro arquivos em `/components/ui/`, um `lib/constants.ts`, e `app/layout.tsx` com SEO completo.
</objective>

<execution_context>
@/Users/marcelomattioli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcelomattioli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/.planning/PROJECT.md
@/Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/.planning/REQUIREMENTS.md
@/Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/.planning/phases/01-foundation/01-UI-SPEC.md
@/Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/CLAUDE.md
@/Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Contratos normativos da UI-SPEC.md que o executor deve implementar exatamente -->

Eyebrow props (TypeScript):
```ts
type EyebrowProps = {
  children: React.ReactNode;
  tone?: "light" | "dark"; // default: "light"
  className?: string;
};
// tone="light"  → texto: text-pruma-navy-deep (#1E3080)
// tone="dark"   → texto: text-pruma-cyan (#00AEEF)
// Linha horizontal: w-8 h-px bg-pruma-cyan (32×1px, sempre ciano)
// Gap: gap-3 (12px) entre linha e texto
// Texto: font-mono text-xs font-normal uppercase tracking-eyebrow (12px, 400, 0.12em)
// Container: inline-flex items-center
```

Button props (TypeScript):
```ts
type ButtonProps = {
  variant?: "primary" | "ghost"; // default: "primary"
  href?: string;
  iconLeft?: React.ReactNode;
  iconRight?: React.ReactNode;
  children: React.ReactNode;
  className?: string;
  disabled?: boolean;
} & React.ButtonHTMLAttributes<HTMLButtonElement>;
// Primary: bg-pruma-navy text-white hover:bg-pruma-navy-mid
// Primary hover: -translate-y-1 shadow-pruma-cyan
// Ghost: bg-transparent text-pruma-navy border border-pruma-navy hover:bg-pruma-cyan-pale
// Ghost hover: -translate-y-1
// Comum: px-6 py-3.5 rounded-pruma-sm font-sans text-sm font-medium min-h-[44px]
// Disabled: opacity-50 cursor-not-allowed (sem hover transforms)
// Focus: focus-visible:ring-2 focus-visible:ring-pruma-cyan focus-visible:ring-offset-2 focus-visible:outline-none
// Transition: transition-[transform,box-shadow,background-color,border-color] duration-200 ease-out
// Quando href fornecido: renderizar como Next.js <Link> com mesma aparência
```

Card props (TypeScript):
```ts
type CardProps = {
  interactive?: boolean; // default: true — habilita hover transform
  as?: "div" | "article" | "li"; // default: "div"
  children: React.ReactNode;
  className?: string;
};
// Resting: bg-white border border-pruma-gray-soft rounded-pruma-md p-8 shadow-pruma-sm
// Hover (quando interactive=true): -translate-y-0.5 border-pruma-cyan-light
// Transition: transition-[transform,border-color] duration-250 ease-out (250ms)
// Cursor: cursor-pointer quando interactive=true
```

Container props (TypeScript):
```ts
type ContainerProps = {
  as?: "div" | "section" | "article" | "main"; // default: "div"
  children: React.ReactNode;
  className?: string;
};
// max-w-6xl mx-auto px-6 md:px-8
// SEM background, SEM py-*
// NÃO pode ter Container dentro de Container
```

SEO metadata (app/layout.tsx):
```ts
export const metadata: Metadata = {
  title: "Pruma | App Web de Força de Vendas B2B para Indústrias e Distribuidoras",
  description: "Governe sua operação comercial B2B com visibilidade total — do vendedor em campo ao conselho. App web para indústrias e distribuidoras.",
  openGraph: {
    title: "Pruma | App Web de Força de Vendas B2B para Indústrias e Distribuidoras",
    description: "Governe sua operação comercial B2B com visibilidade total — do vendedor em campo ao conselho. App web para indústrias e distribuidoras.",
    type: "website",
    locale: "pt_BR",
    siteName: "Pruma",
  },
  twitter: {
    card: "summary_large_image",
    title: "Pruma | App Web de Força de Vendas B2B para Indústrias e Distribuidoras",
    description: "Governe sua operação comercial B2B com visibilidade total — do vendedor em campo ao conselho. App web para indústrias e distribuidoras.",
  },
};
```

lib/constants.ts strings canônicas:
```ts
export const SITE_TITLE = "Pruma | App Web de Força de Vendas B2B para Indústrias e Distribuidoras";
export const SITE_DESCRIPTION = "Governe sua operação comercial B2B com visibilidade total — do vendedor em campo ao conselho. App web para indústrias e distribuidoras.";
export const CTA_PRIMARY = "Falar com um consultor";     // Navbar, Hero, CTA Final
export const CTA_SECONDARY = "Conhecer o app";           // Hero ghost button
export const CTA_FORM = "Agendar conversa com consultor"; // Botão de submit do formulário
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEO metadata + lib/constants.ts com strings canônicas</name>
  <read_first>
    - /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/.planning/phases/01-foundation/01-UI-SPEC.md (seção "Copywriting Contract" e "Phase 1 Acceptance Criteria" item 9)
    - /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/.planning/REQUIREMENTS.md (SETUP-05)
    - /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/app/layout.tsx (estado atual após Plano 01)
  </read_first>
  <files>app/layout.tsx, lib/constants.ts</files>
  <action>
**1. Criar `lib/constants.ts`** (criar diretório `lib/` se não existir):

```ts
// lib/constants.ts
// Strings canônicas da LP Pruma — editar aqui para atualizar todo o site

/** Título da página e meta Open Graph */
export const SITE_TITLE =
  "Pruma | App Web de Força de Vendas B2B para Indústrias e Distribuidoras";

/** Meta description (~150 chars) */
export const SITE_DESCRIPTION =
  "Governe sua operação comercial B2B com visibilidade total — do vendedor em campo ao conselho. App web para indústrias e distribuidoras.";

// ─── CTAs ─────────────────────────────────────────────────────────────────
/** CTA principal: aparece em Navbar, Hero e CTA Final (mínimo 3 ocorrências) */
export const CTA_PRIMARY = "Falar com um consultor";

/** CTA secundário: botão ghost no Hero */
export const CTA_SECONDARY = "Conhecer o app";

/** CTA do formulário CTA Final */
export const CTA_FORM = "Agendar conversa com consultor";

// ─── Estados do formulário ─────────────────────────────────────────────────
/** Texto de instrução do formulário vazio */
export const FORM_EMPTY_STATE =
  "Preencha os campos abaixo para conversar com um consultor.";

/** Mensagem de sucesso após envio do formulário */
export const FORM_SUCCESS =
  "Recebemos. Em breve um consultor da Pruma entra em contato.";
```

**2. Atualizar `app/layout.tsx`** — substituir o objeto `metadata` pelo SEO completo, importando de `lib/constants.ts`:

```tsx
import type { Metadata } from "next";
import { Fraunces, Inter, JetBrains_Mono } from "next/font/google";
import { SITE_TITLE, SITE_DESCRIPTION } from "@/lib/constants";
import "./globals.css";

const fraunces = Fraunces({
  subsets: ["latin"],
  variable: "--font-fraunces",
  weight: ["400", "500", "600"],
  display: "swap",
});

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  weight: ["400", "500"],
  display: "swap",
});

const mono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-mono",
  weight: ["400"],
  display: "swap",
});

export const metadata: Metadata = {
  title: SITE_TITLE,
  description: SITE_DESCRIPTION,
  openGraph: {
    title: SITE_TITLE,
    description: SITE_DESCRIPTION,
    type: "website",
    locale: "pt_BR",
    siteName: "Pruma",
  },
  twitter: {
    card: "summary_large_image",
    title: SITE_TITLE,
    description: SITE_DESCRIPTION,
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="pt-BR"
      className={`${fraunces.variable} ${inter.variable} ${mono.variable}`}
    >
      <body className="font-sans antialiased">{children}</body>
    </html>
  );
}
```

IMPORTANTE: manter os imports de fonte e variáveis idênticos ao Plano 01. Apenas o objeto `metadata` e o novo import de `lib/constants.ts` são mudanças neste task.
  </action>
  <verify>
    <automated>cd /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas && grep -c "Pruma | App Web de Força de Vendas B2B para Indústrias e Distribuidoras" app/layout.tsx && grep -c "CTA_PRIMARY" lib/constants.ts && grep -c "Falar com um consultor" lib/constants.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/layout.tsx` contém a string exata `"Pruma | App Web de Força de Vendas B2B para Indústrias e Distribuidoras"` via importação de `SITE_TITLE`
    - `app/layout.tsx` contém `openGraph` com `locale: "pt_BR"` e `type: "website"`
    - `app/layout.tsx` contém `twitter` com `card: "summary_large_image"`
    - `lib/constants.ts` exporta `SITE_TITLE`, `SITE_DESCRIPTION`, `CTA_PRIMARY`, `CTA_SECONDARY`, `CTA_FORM`, `FORM_EMPTY_STATE`, `FORM_SUCCESS`
    - `lib/constants.ts` contém `"Falar com um consultor"` como valor de `CTA_PRIMARY`
    - `lib/constants.ts` contém `"Agendar conversa com consultor"` como valor de `CTA_FORM`
    - `lib/constants.ts` NÃO contém palavras banidas: "Venda mais", "Plataforma all-in-one", "Automatize"
    - `npm run build` não gera erros de TypeScript em `app/layout.tsx` ou `lib/constants.ts`
  </acceptance_criteria>
  <done>Metadata SEO completa em layout.tsx importando de lib/constants.ts, com title exato, description ~150 chars, e Open Graph + Twitter cards configurados</done>
</task>

<task type="auto">
  <name>Task 2: Implementar os quatro componentes UI primitivos (Eyebrow, Button, Card, Container)</name>
  <read_first>
    - /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/.planning/phases/01-foundation/01-UI-SPEC.md (seção completa "Component Primitives — Visual Contract")
    - /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/tailwind.config.ts (tokens disponíveis após Plano 01)
  </read_first>
  <files>components/ui/Eyebrow.tsx, components/ui/Button.tsx, components/ui/Card.tsx, components/ui/Container.tsx</files>
  <action>
Criar o diretório `components/ui/` e os quatro arquivos a seguir. Cada implementação deve usar EXCLUSIVAMENTE os tokens Tailwind `pruma.*` definidos no `tailwind.config.ts` — nunca hexes inline, nunca `style={{}}` para cores ou espaçamentos já tokenizados.

---

**`components/ui/Eyebrow.tsx`:**

```tsx
import { cn } from "@/lib/utils";

type EyebrowProps = {
  children: React.ReactNode;
  tone?: "light" | "dark";
  className?: string;
};

export function Eyebrow({ children, tone = "light", className }: EyebrowProps) {
  return (
    <div
      className={cn(
        "inline-flex items-center gap-3 mb-4",
        className
      )}
    >
      {/* Linha horizontal ciano — sempre ciano independente do tone */}
      <span className="w-eyebrow-line h-px bg-pruma-cyan flex-shrink-0" aria-hidden="true" />
      {/* Texto UPPERCASE JetBrains Mono 12px tracking 0.12em */}
      <span
        className={cn(
          "font-mono text-xs font-normal uppercase tracking-eyebrow",
          tone === "dark" ? "text-pruma-cyan" : "text-pruma-navy-deep"
        )}
      >
        {children}
      </span>
    </div>
  );
}
```

Nota: requer `lib/utils.ts` com a função `cn`. Criar `lib/utils.ts` se não existir:

```ts
// lib/utils.ts
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

---

**`components/ui/Button.tsx`:**

```tsx
import Link from "next/link";
import { cn } from "@/lib/utils";
import { ButtonHTMLAttributes } from "react";

type ButtonProps = {
  variant?: "primary" | "ghost";
  href?: string;
  iconLeft?: React.ReactNode;
  iconRight?: React.ReactNode;
  children: React.ReactNode;
  className?: string;
  disabled?: boolean;
} & ButtonHTMLAttributes<HTMLButtonElement>;

const baseStyles =
  "inline-flex items-center justify-center gap-2 font-sans text-sm font-medium min-h-[44px] px-6 py-3.5 rounded-pruma-sm transition-[transform,box-shadow,background-color,border-color] duration-200 ease-out focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-pruma-cyan focus-visible:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed";

const variantStyles = {
  primary:
    "bg-pruma-navy text-white shadow-pruma-sm hover:bg-pruma-navy-mid hover:-translate-y-1 hover:shadow-pruma-cyan active:translate-y-0 active:shadow-pruma-sm active:bg-pruma-navy disabled:hover:translate-y-0 disabled:hover:shadow-pruma-sm disabled:hover:bg-pruma-navy",
  ghost:
    "bg-transparent text-pruma-navy border border-pruma-navy hover:bg-pruma-cyan-pale hover:-translate-y-1 active:translate-y-0 active:bg-pruma-cyan-pale disabled:hover:translate-y-0 disabled:hover:bg-transparent",
};

export function Button({
  variant = "primary",
  href,
  iconLeft,
  iconRight,
  children,
  className,
  disabled,
  ...props
}: ButtonProps) {
  const classes = cn(baseStyles, variantStyles[variant], className);

  if (href) {
    return (
      <Link href={href} className={classes} aria-disabled={disabled}>
        {iconLeft && <span className="flex-shrink-0 w-4 h-4">{iconLeft}</span>}
        {children}
        {iconRight && <span className="flex-shrink-0 w-4 h-4">{iconRight}</span>}
      </Link>
    );
  }

  return (
    <button
      className={classes}
      disabled={disabled}
      {...props}
    >
      {iconLeft && <span className="flex-shrink-0 w-4 h-4">{iconLeft}</span>}
      {children}
      {iconRight && <span className="flex-shrink-0 w-4 h-4">{iconRight}</span>}
    </button>
  );
}
```

---

**`components/ui/Card.tsx`:**

```tsx
import { cn } from "@/lib/utils";

type CardProps = {
  interactive?: boolean;
  as?: "div" | "article" | "li";
  children: React.ReactNode;
  className?: string;
};

export function Card({
  interactive = true,
  as: Tag = "div",
  children,
  className,
}: CardProps) {
  return (
    <Tag
      className={cn(
        "bg-white border border-pruma-gray-soft rounded-pruma-md p-8 shadow-pruma-sm",
        interactive &&
          "transition-[transform,border-color] duration-[250ms] ease-out cursor-pointer hover:-translate-y-0.5 hover:border-pruma-cyan-light",
        className
      )}
    >
      {children}
    </Tag>
  );
}
```

---

**`components/ui/Container.tsx`:**

```tsx
import { cn } from "@/lib/utils";

type ContainerProps = {
  as?: "div" | "section" | "article" | "main";
  children: React.ReactNode;
  className?: string;
};

export function Container({
  as: Tag = "div",
  children,
  className,
}: ContainerProps) {
  return (
    <Tag className={cn("max-w-6xl mx-auto px-6 md:px-8", className)}>
      {children}
    </Tag>
  );
}
```

---

PROIBIDO em qualquer primitivo:
- Gradientes
- `rounded-full` (pill)
- Cores inline via `style={{}}` quando o token Tailwind existe
- Background tints em Card (`#E0F6FE` é fundo de seção, não de card)
- `py-*` em Container
- Nesting de Container dentro de Container
  </action>
  <verify>
    <automated>cd /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas && ls components/ui/ && grep -l "export function" components/ui/*.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `components/ui/Eyebrow.tsx` existe e exporta a função `Eyebrow`
    - `components/ui/Button.tsx` existe e exporta a função `Button`
    - `components/ui/Card.tsx` existe e exporta a função `Card`
    - `components/ui/Container.tsx` existe e exporta a função `Container`
    - `lib/utils.ts` existe e exporta a função `cn`
    - `components/ui/Eyebrow.tsx` contém `w-eyebrow-line h-px bg-pruma-cyan` (linha ciano)
    - `components/ui/Eyebrow.tsx` contém `font-mono text-xs font-normal uppercase tracking-eyebrow`
    - `components/ui/Eyebrow.tsx` contém `text-pruma-navy-deep` (tone light) e `text-pruma-cyan` (tone dark)
    - `components/ui/Button.tsx` contém `bg-pruma-navy` e `bg-pruma-navy-mid` (primary)
    - `components/ui/Button.tsx` contém `shadow-pruma-cyan` (primary hover glow)
    - `components/ui/Button.tsx` contém `hover:bg-pruma-cyan-pale` (ghost hover)
    - `components/ui/Button.tsx` contém `rounded-pruma-sm` e `min-h-[44px]`
    - `components/ui/Button.tsx` contém `focus-visible:ring-pruma-cyan`
    - `components/ui/Button.tsx` contém `disabled:opacity-50 disabled:cursor-not-allowed`
    - `components/ui/Button.tsx` NÃO contém `rounded-full`
    - `components/ui/Card.tsx` contém `rounded-pruma-md` e `border-pruma-gray-soft`
    - `components/ui/Card.tsx` contém `hover:border-pruma-cyan-light` e `hover:-translate-y-0.5`
    - `components/ui/Card.tsx` NÃO contém `bg-pruma-cyan-pale` (fundo de seção proibido em card)
    - `components/ui/Container.tsx` contém `max-w-6xl mx-auto px-6 md:px-8`
    - `components/ui/Container.tsx` NÃO contém `py-`, `bg-`, ou outro padding/background próprio
    - `npm run build` não gera erros TypeScript em nenhum dos quatro arquivos
  </acceptance_criteria>
  <done>Quatro componentes UI primitivos implementados com fidelidade exata à UI-SPEC: Eyebrow com linha ciano + Mono UPPERCASE, Button primary/ghost com todos os estados corretos, Card com hover interativo, Container layout-only sem background</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Consumer → Button(href) | href recebido como prop pode conter URLs externas |
| User input → constants.ts | Strings de copy são estáticas — sem input do usuário neste plano |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Button href prop | mitigate | Usar Next.js `<Link>` (não `<a>` raw) — Next.js valida relative paths; hrefs externos passam pela SPA como links normais. Âncoras `#section` são válidas. Não há input dinâmico do usuário neste componente. |
| T-02-02 | Information Disclosure | metadata SEO | accept | Description de 150 chars é copy de marketing intencional — sem dados sensíveis |
| T-02-03 | Tampering | lib/constants.ts | accept | Arquivo estático em repositório; não aceita input de usuário em runtime |
| T-02-04 | Denial of Service | font-family fallbacks | accept | globals.css já define fallbacks system-ui/Georgia/ui-monospace; primitivos herdam |
| T-02-05 | Elevation of Privilege | disabled Button com href | mitigate | Quando `href` fornecido, aplicar `aria-disabled` e prevenir clique via CSS `pointer-events-none` quando `disabled=true` |
</threat_model>

<verification>
Após execução das duas tasks:

```bash
cd /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas

# 1. SEO metadata correto
grep -c "Pruma | App Web de Força de Vendas B2B" app/layout.tsx
grep -c "pt_BR" app/layout.tsx
grep -c "summary_large_image" app/layout.tsx

# 2. constants.ts com strings canônicas
grep -c "CTA_PRIMARY" lib/constants.ts
grep -c "Falar com um consultor" lib/constants.ts
grep -c "Agendar conversa com consultor" lib/constants.ts

# 3. Todos os quatro componentes existem e exportam
grep -l "export function" components/ui/*.tsx | wc -l

# 4. Tokens críticos nos primitivos
grep -c "w-eyebrow-line" components/ui/Eyebrow.tsx
grep -c "tracking-eyebrow" components/ui/Eyebrow.tsx
grep -c "rounded-pruma-sm" components/ui/Button.tsx
grep -c "shadow-pruma-cyan" components/ui/Button.tsx
grep -c "rounded-pruma-md" components/ui/Card.tsx
grep -c "max-w-6xl" components/ui/Container.tsx

# 5. Build sem erros
npm run build 2>&1 | tail -10
```
</verification>

<success_criteria>
- `app/layout.tsx` declara metadata completa com title exato `"Pruma | App Web de Força de Vendas B2B para Indústrias e Distribuidoras"`, openGraph com locale `pt_BR`, e twitter card `summary_large_image`
- `lib/constants.ts` exporta 7 constantes sem palavras banidas
- `components/ui/Eyebrow.tsx` implementado com linha 32px ciano, Mono UPPERCASE 12px, tracking 0.12em, suporte a tone light/dark
- `components/ui/Button.tsx` implementado com variants primary/ghost, hover transforms, glow ciano, min-height 44px, disabled state, focus ring ciano, suporte a href via Link
- `components/ui/Card.tsx` implementado com hover border cyan-light e translateY(-2px), sem background tints
- `components/ui/Container.tsx` implementado como max-w-6xl sem background próprio
- `npm run build` completa sem erros TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-foundation/01-02-SUMMARY.md` com:
- Arquivos criados/modificados
- Props TypeScript de cada primitivo
- Qualquer desvio intencional da UI-SPEC (deve ser zero)
- Estado do `npm run build`
</output>
