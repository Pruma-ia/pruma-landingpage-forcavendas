---
phase: 03-full-lp-animations
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - components/Funcionalidades.tsx
  - components/Footer.tsx
autonomous: true
requirements: [SECT-07, SECT-12]
must_haves:
  truths:
    - "Funcionalidades section renders a bento grid with 1 hero card (full-width, embedding scaled PainelDashboard) + 5 smaller cards in a 3-col grid below — no overflow"
    - "Footer renders bg-pruma-navy with PRUMA wordmark, 3 columns of links (PRODUTO / EMPRESA / CONTATO), and a copyright baseline"
    - "Both sections consume copy from FUNCIONALIDADES / FOOTER exports"
  artifacts:
    - path: "components/Funcionalidades.tsx"
      provides: "SECT-07 bento grid section"
      contains: "lg:col-span-3"
    - path: "components/Footer.tsx"
      provides: "SECT-12 footer with 3 link columns"
      contains: "bg-pruma-navy"
  key_links:
    - from: "components/Funcionalidades.tsx"
      to: "components/mockups/PainelDashboard"
      via: "import (scaled embed in hero card)"
      pattern: "import \\{ PainelDashboard \\}"
    - from: "components/Footer.tsx"
      to: "lib/constants.ts (FOOTER, BRAND_NAME)"
      via: "named imports"
      pattern: "import \\{ BRAND_NAME, FOOTER \\}"
---

<objective>
Implement SECT-07 (Funcionalidades — bento grid: 1 hero card embedding the PainelDashboard at scale + 5 smaller feature cards) and SECT-12 (Footer — dark navy footer with brand wordmark, 3 link columns, copyright).

Purpose: SECT-07 is the visual climax of the feature pitch — the dashboard reappears as the "hero" of the bento, surrounded by 5 supporting capabilities. SECT-12 closes the page in a dark tonal mirror of SECT-09, providing nav-link discoverability and the legal baseline.

Output: 2 new section components ready to be wired into app/page.tsx in Plan 03-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-full-lp-animations/03-CONTEXT.md
@.planning/phases/03-full-lp-animations/03-PATTERNS.md
@.planning/phases/03-full-lp-animations/03-UI-SPEC.md
@CLAUDE.md
@components/Diagnostico.tsx
@components/Navbar.tsx
@components/ui/Container.tsx
@components/ui/Eyebrow.tsx
@components/ui/Card.tsx
@components/mockups/PainelDashboard.tsx
@lib/constants.ts

<interfaces>
From lib/constants.ts (after Plan 03-01):
- `FUNCIONALIDADES`: { eyebrow, headline, subheadline, hero: {title, description}, cards: [{title, description, icon}] (5 items, icons "Smartphone"|"Tag"|"BarChart2"|"AlertTriangle"|"FileText") }
- `FOOTER`: { columns: [{heading, links: [{label, href}]}] (3 items), copyright }
- `BRAND_NAME`: "PRUMA"

From components/ui/Card.tsx — `interactive` prop toggles hover translate-y -2px + cyan border (D-10).

From components/mockups/PainelDashboard.tsx — exported as `PainelDashboard`, widescreen mockup. We will visually scale it via `transform: scale(0.7)` per CONTEXT.md "Specifics" — bento hero card.

Tailwind:
- `lg:col-span-3` to make hero card full-width in a 3-col grid
- `overflow-hidden` on the scaled wrapper to clip the down-scaled mockup to the card boundary
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create components/Funcionalidades.tsx (SECT-07 — bento grid)</name>
  <files>components/Funcionalidades.tsx</files>
  <read_first>
    - components/Diagnostico.tsx (analog — 3-col grid + Card map)
    - components/ui/Card.tsx (verify props — interactive, as, className override)
    - components/mockups/PainelDashboard.tsx (verify export + intrinsic sizing — needs scale 0.7 wrapper)
    - lib/constants.ts (verify FUNCIONALIDADES shape after Plan 03-01)
    - .planning/phases/03-full-lp-animations/03-PATTERNS.md (lines 137-180 — exact bento pattern, including hero card structure)
    - .planning/phases/03-full-lp-animations/03-CONTEXT.md (D-01, D-02, D-03, D-04, "Specifics" — bento hero card)
  </read_first>
  <action>
    Create `components/Funcionalidades.tsx` as a Server Component (NO `"use client"`).

    Bento layout per D-01: 1 hero card spanning full width (lg:col-span-3) on top + 5 smaller cards in a 3-column grid below (which means the 5 cards distribute 3+2 across two rows).

    Structure:
    ```tsx
    import {
      Smartphone, Tag, BarChart2, AlertTriangle, FileText,
      type LucideIcon,
    } from "lucide-react";
    import { Container } from "@/components/ui/Container";
    import { Eyebrow } from "@/components/ui/Eyebrow";
    import { Card } from "@/components/ui/Card";
    import { PainelDashboard } from "@/components/mockups/PainelDashboard";
    import { FUNCIONALIDADES } from "@/lib/constants";

    const iconMap: Record<
      "Smartphone" | "Tag" | "BarChart2" | "AlertTriangle" | "FileText",
      LucideIcon
    > = { Smartphone, Tag, BarChart2, AlertTriangle, FileText };

    export function Funcionalidades() {
      return (
        <section
          id="funcionalidades"
          aria-labelledby="funcionalidades-heading"
          className="bg-pruma-cyan-pale py-section-y-mobile lg:py-section-y-desktop"
        >
          <Container as="div">
            <Eyebrow tone="light">{FUNCIONALIDADES.eyebrow}</Eyebrow>

            <h2
              id="funcionalidades-heading"
              className="font-serif text-4xl lg:text-[52px] font-medium text-pruma-navy leading-[1.10] tracking-tight max-w-[20ch]"
            >
              {FUNCIONALIDADES.headline}
            </h2>

            <p className="font-sans text-base lg:text-lg text-pruma-gray-text leading-[1.6] mt-4 max-w-[60ch]">
              {FUNCIONALIDADES.subheadline}
            </p>

            <ul className="grid grid-cols-1 lg:grid-cols-3 gap-4 lg:gap-6 mt-12 lg:mt-16 list-none">
              {/* Hero card — full-width, embeds scaled PainelDashboard */}
              <Card as="li" interactive={false} className="lg:col-span-3 overflow-hidden p-0">
                <div className="p-8 pb-0">
                  <h3 className="font-serif text-2xl lg:text-3xl font-semibold text-pruma-navy leading-tight">
                    {FUNCIONALIDADES.hero.title}
                  </h3>
                  <p className="font-sans text-base text-pruma-gray-text mt-3 max-w-[60ch]">
                    {FUNCIONALIDADES.hero.description}
                  </p>
                </div>
                {/* Mockup window — scale 0.7 per CONTEXT.md Specifics */}
                <div className="overflow-hidden mt-6" style={{ height: "320px" }}>
                  <div style={{ transform: "scale(0.7)", transformOrigin: "top center" }}>
                    <PainelDashboard />
                  </div>
                </div>
              </Card>

              {/* 5 smaller cards */}
              {FUNCIONALIDADES.cards.map((card) => {
                const Icon = iconMap[card.icon];
                return (
                  <Card key={card.title} as="li" interactive>
                    <Icon className="w-5 h-5 text-pruma-navy" strokeWidth={1.5} aria-hidden="true" />
                    <h3 className="font-serif text-lg font-semibold text-pruma-navy mt-5 leading-tight">
                      {card.title}
                    </h3>
                    <p className="font-sans text-sm text-pruma-gray-text leading-[1.55] mt-2">
                      {card.description}
                    </p>
                  </Card>
                );
              })}
            </ul>
          </Container>
        </section>
      );
    }
    ```

    Rules:
    - Background MUST be `bg-pruma-cyan-pale` (#E0F6FE) — per REQUIREMENTS SECT-07.
    - Section `id="funcionalidades"` matches NAV_LINKS href.
    - Hero card uses `interactive={false}` (it's a display anchor, not a clickable target). Override `p-0` so we can split internal padding.
    - Smaller cards use `interactive` (true) to inherit D-10 hover state.
    - Icons: 20px (w-5 h-5), strokeWidth 1.5 — per D-03.
    - Mockup scaled to 0.7 with `overflow-hidden` clipping; this is the ONLY place inline `style={{ transform: "scale(...)" }}` is allowed (per PATTERNS.md "no inline styles except transform: scale for mockup window").
    - No "use client", no hooks, no events. Server Component only.
    - No bento overflow at lg+ breakpoints — `lg:grid-cols-3` + `lg:col-span-3` = 1 row hero + (3+2) = 2 rows of small cards.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -10 &amp;&amp; grep -c 'lg:col-span-3' components/Funcionalidades.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `components/Funcionalidades.tsx` exists
    - `grep -c '^"use client"' components/Funcionalidades.tsx` returns 0
    - `grep -c 'export function Funcionalidades' components/Funcionalidades.tsx` returns 1
    - `grep -c 'id="funcionalidades"' components/Funcionalidades.tsx` returns 1
    - `grep -c 'bg-pruma-cyan-pale' components/Funcionalidades.tsx` returns 1
    - `grep -c 'import { PainelDashboard }' components/Funcionalidades.tsx` returns 1
    - `grep -c 'import { FUNCIONALIDADES }' components/Funcionalidades.tsx` returns 1
    - `grep -c 'lg:col-span-3' components/Funcionalidades.tsx` returns 1 (hero card)
    - `grep -c 'transform: "scale(0.7)"' components/Funcionalidades.tsx` returns 1
    - `grep -c 'transformOrigin: "top center"' components/Funcionalidades.tsx` returns 1
    - `grep -c 'FUNCIONALIDADES.cards.map' components/Funcionalidades.tsx` returns 1
    - `grep -cE 'Smartphone|Tag|BarChart2|AlertTriangle|FileText' components/Funcionalidades.tsx` returns at least 5 (one per icon import + at least the imports)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    SECT-07 bento grid renders with 1 full-width hero card (containing FUNCIONALIDADES.hero copy + a scaled PainelDashboard mockup window) and 5 interactive smaller cards in a 3-col grid. Build passes with zero overflow at lg+ breakpoints.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create components/Footer.tsx (SECT-12)</name>
  <files>components/Footer.tsx</files>
  <read_first>
    - components/Navbar.tsx (analog — Container + BRAND_NAME + Link with hover transition)
    - lib/constants.ts (verify FOOTER + BRAND_NAME shapes)
    - .planning/phases/03-full-lp-animations/03-PATTERNS.md (lines 416-462 — exact Footer pattern)
    - .planning/phases/03-full-lp-animations/03-CONTEXT.md ("SECT-12 Footer")
  </read_first>
  <action>
    Create `components/Footer.tsx` as a Server Component (NO `"use client"`).

    Structure:
    ```tsx
    import Link from "next/link";
    import { Container } from "@/components/ui/Container";
    import { BRAND_NAME, FOOTER } from "@/lib/constants";

    export function Footer() {
      return (
        <footer className="bg-pruma-navy py-section-y-mobile lg:py-section-y-desktop">
          <Container as="div">
            {/* Brand wordmark */}
            <span className="font-serif font-semibold text-xl text-white tracking-tight">
              {BRAND_NAME}
            </span>

            {/* 3-column nav */}
            <div className="grid grid-cols-1 md:grid-cols-3 gap-10 lg:gap-12 mt-10 lg:mt-12">
              {FOOTER.columns.map((col) => (
                <div key={col.heading}>
                  <p className="font-mono text-xs uppercase tracking-eyebrow text-pruma-cyan mb-4">
                    {col.heading}
                  </p>
                  <ul className="flex flex-col gap-3 list-none">
                    {col.links.map((link) => (
                      <li key={link.label}>
                        <Link
                          href={link.href}
                          className="font-sans text-sm text-white/70 hover:text-white transition-colors duration-150 ease-out focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-pruma-cyan focus-visible:ring-offset-2 rounded-pruma-sm"
                        >
                          {link.label}
                        </Link>
                      </li>
                    ))}
                  </ul>
                </div>
              ))}
            </div>

            {/* Copyright baseline */}
            <div className="border-t border-white/10 mt-12 pt-8">
              <p className="font-sans text-xs text-white/40">{FOOTER.copyright}</p>
            </div>
          </Container>
        </footer>
      );
    }
    ```

    Rules:
    - Background MUST be `bg-pruma-navy` (matches SECT-09 dark tonal — REQUIREMENTS SECT-12).
    - Outer element `<footer>` (semantic HTML) — NOT a `<section>`.
    - Brand wordmark white, Fraunces font.
    - Column headings: cyan, mono, uppercase (eyebrow visual treatment, no eyebrow component since we want it inline above each list).
    - Links: `text-white/70 hover:text-white` with `transition-colors duration-150 ease-out`. Min focus-visible ring for a11y.
    - All link hrefs come from FOOTER.columns[].links[].href — do NOT hardcode anchors. External links (e.g., LinkedIn, mailto:) are valid hrefs in next/link.
    - Server Component only.
    - NO emojis. NO drop shadows on the dark surface.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -10 &amp;&amp; grep -c 'bg-pruma-navy' components/Footer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `components/Footer.tsx` exists
    - `grep -c '^"use client"' components/Footer.tsx` returns 0
    - `grep -c 'export function Footer' components/Footer.tsx` returns 1
    - `grep -c '<footer' components/Footer.tsx` returns 1
    - `grep -c 'bg-pruma-navy' components/Footer.tsx` returns 1
    - `grep -c 'import { BRAND_NAME, FOOTER }\|import { BRAND_NAME, FOOTER}' components/Footer.tsx` returns 1
    - `grep -c 'FOOTER.columns.map' components/Footer.tsx` returns 1
    - `grep -c 'FOOTER.copyright' components/Footer.tsx` returns 1
    - `grep -c 'text-pruma-cyan' components/Footer.tsx` returns at least 1 (column headings)
    - `grep -c 'text-white/70' components/Footer.tsx` returns at least 1 (link default state)
    - `grep -c 'border-t border-white/10' components/Footer.tsx` returns 1 (copyright divider)
    - `grep -ciE 'bg-gradient|from-|to-' components/Footer.tsx` returns 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    SECT-12 Footer renders bg-pruma-navy with PRUMA wordmark, 3 columns of links pulled from FOOTER constant, and a copyright baseline. Server Component, no gradients, build passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time → Bundle | Static content. The only "external" surface is footer links pointing to external URLs (e.g., LinkedIn, mailto). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T (Tampering) | Funcionalidades.tsx, Footer.tsx | mitigate | Copy interpolated from typed constants. No `dangerouslySetInnerHTML`. React auto-escapes. Verified via `grep -c "dangerouslySetInnerHTML" components/Funcionalidades.tsx components/Footer.tsx` = 0. |
| T-03-08 | T (Tampering) | Footer external links | mitigate | All hrefs come from `FOOTER.columns[].links[].href` (typed constant). No user-supplied URLs. mailto: and https: schemes only — verified by reviewer reading the constant. External `<Link>` to https domains is rendered by next/link with no `rel` opener risk on same-origin app shell. If FOOTER.copy includes external `https://` links, reviewer should add `rel="noopener noreferrer"` only if `target="_blank"` is added later (not added in this plan). |
| T-03-09 | I (Information disclosure) | Funcionalidades hero card mockup | accept | Reuses existing PainelDashboard with fictional data — already accepted Phase 2. |
</threat_model>

<verification>
- Both files exist; `npm run build` exits 0
- Funcionalidades has bg-pruma-cyan-pale; Footer has bg-pruma-navy
- Inline `transform: scale(0.7)` appears EXACTLY once in Funcionalidades (the bento hero mockup window — only allowed inline-style location)
- No "use client", no hooks
- All copy + links come from constants
</verification>

<success_criteria>
SECT-07 delivers the bento grid with the hero card showing the live PainelDashboard scaled to 0.7. SECT-12 closes the LP with brand + nav + copyright. Both ready to be wired into app/page.tsx in Plan 03-07.
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-lp-animations/03-04-SUMMARY.md` documenting the bento layout decisions, mockup scaling approach, and footer link inventory.
</output>
