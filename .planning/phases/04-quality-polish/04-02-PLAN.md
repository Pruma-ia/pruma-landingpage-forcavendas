---
phase: 04-quality-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/constants.ts
  - components/Navbar.tsx
  - components/Hero.tsx
  - components/CTAFinal.tsx
  - components/Footer.tsx
  - components/Diagnostico.tsx
  - components/Tese.tsx
  - components/ComoFunciona.tsx
  - components/PainelGestor.tsx
  - components/Funcionalidades.tsx
  - components/ParaQuem.tsx
  - components/Diferencial.tsx
  - components/ProvaSocial.tsx
autonomous: true
requirements:
  - QA-04
  - QA-05

must_haves:
  truths:
    - "Toda string de copy visível ao usuário final vem de lib/constants.ts — nenhuma string hardcoded em componente"
    - "lib/constants.ts tem comentário /* EDITÁVEL: ... */ em cada campo que o cliente pode precisar trocar"
    - "CTA 'Falar com um consultor' aparece literalmente em pelo menos 3 locais distintos: Navbar, Hero e Footer"
    - "Nenhum componente importa strings literais de copy sem passar por constants.ts"
  artifacts:
    - path: "lib/constants.ts"
      provides: "Fonte única de copy com comentários EDITÁVEL"
      contains: "EDITÁVEL"
    - path: "components/Navbar.tsx"
      provides: "Consome CTA_PRIMARY de constants"
      contains: "CTA_PRIMARY"
    - path: "components/Hero.tsx"
      provides: "Consome CTA_PRIMARY de constants"
      contains: "CTA_PRIMARY"
    - path: "components/Footer.tsx"
      provides: "Consome CTA_PRIMARY de constants"
      contains: "CTA_PRIMARY"
  key_links:
    - from: "components/Navbar.tsx"
      to: "lib/constants.ts"
      via: "import { CTA_PRIMARY }"
      pattern: "CTA_PRIMARY"
    - from: "components/Hero.tsx"
      to: "lib/constants.ts"
      via: "import { CTA_PRIMARY }"
      pattern: "CTA_PRIMARY"
    - from: "components/Footer.tsx"
      to: "lib/constants.ts"
      via: "import { CTA_PRIMARY }"
      pattern: "CTA_PRIMARY"
---

<objective>
Auditar todos os componentes em busca de strings de copy hardcoded fora de lib/constants.ts, mover qualquer string encontrada para constants.ts com comentários `/* EDITÁVEL: ... */`, e verificar que o CTA "Falar com um consultor" está presente em exatamente 3 touchpoints distintos (Navbar, Hero, Footer).

Purpose: O requisito QA-04 exige que o cliente possa editar qualquer string da LP tocando apenas um arquivo. QA-05 exige 3 CTAs distintos para captura de leads em pontos críticos do funil.
Output: lib/constants.ts como fonte única de verdade, com anotações que permitem edição sem conhecimento de código.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar componentes em busca de copy hardcoded e verificar touchpoints do CTA</name>
  <files>
    - lib/constants.ts
    - components/Navbar.tsx
    - components/Hero.tsx
    - components/CTAFinal.tsx
    - components/Footer.tsx
    - components/Diagnostico.tsx
    - components/Tese.tsx
    - components/ComoFunciona.tsx
    - components/PainelGestor.tsx
    - components/Funcionalidades.tsx
    - components/ParaQuem.tsx
    - components/Diferencial.tsx
    - components/ProvaSocial.tsx
  </files>

  <read_first>
    Leia todos os arquivos listados em `files` antes de fazer qualquer alteração.
    Prioridade de leitura:
    1. lib/constants.ts — entender o que já está centralizado
    2. Cada componente .tsx — procurar strings de copy que NÃO vêm de constants.ts
  </read_first>

  <action>
    PARTE A: AUDITORIA DE COPY HARDCODED

    Execute o grep de diagnóstico:
    ```bash
    grep -rn "\"[A-ZÁÉÍÓÚÃÕÂÊÔÇ][a-záéíóúãõâêôç ]\{10,\}" \
      /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/components/ \
      --include="*.tsx" | grep -v "constants" | grep -v "className" | grep -v "aria-label" | grep -v "//"
    ```

    Para cada match encontrado, avalie se é copy visível ao usuário final:
    - É copy visível (headline, parágrafo, label, placeholder, microcopy)? → MOVER para constants.ts
    - É classe CSS, valor técnico, aria-label técnico? → DEIXAR como está

    STRINGS CONHECIDAS QUE PODEM ESTAR HARDCODED (verificar em cada componente):
    Não há strings hardcoded detectadas na análise pré-planejamento — todos os componentes
    parecem já importar de constants.ts. Confirme o resultado do grep. Se o grep retornar
    zero matches relevantes, a auditoria está completa sem alterações necessárias.

    EXCEÇÕES LEGÍTIMAS (não mover para constants.ts):
    - `"Selecione..."` no CTAFinal.tsx — é placeholder UI técnico, já em constants se existir
    - `"Política de privacidade"` no Footer.tsx — é link de navegação técnico (v2 deferred)
    - Strings de placeholder de mockup (`_AbcChart`, `_BrazilMap`) — são dados visuais técnicos
    - Strings `aria-label` e `aria-labelledby` — são atributos de acessibilidade técnicos

    PARTE B: COMENTÁRIOS EDITÁVEIS EM lib/constants.ts

    Abra lib/constants.ts e adicione comentários `/* EDITÁVEL: <descrição> */` nas linhas que
    representam conteúdo que o cliente pode precisar trocar. Use o padrão abaixo.

    CAMPOS QUE PRECISAM DE COMENTÁRIO /* EDITÁVEL: ... */:

    No bloco HERO:
    ```typescript
    export const HERO = {
      eyebrow: "— APP WEB DE FORÇA DE VENDAS B2B", /* EDITÁVEL: subtítulo acima do headline */
      headlineLine1: "Operações comerciais B2B",   /* EDITÁVEL: primeira linha do headline hero */
      headlineLine2: "que precisam parar de improvisar.", /* EDITÁVEL: segunda linha do headline hero */
      subheadline: "Pruma instala...",             /* EDITÁVEL: parágrafo de suporte abaixo do headline */
      microcopy: "SEM CONTRATO DE FIDELIDADE...",  /* EDITÁVEL: microcopy abaixo dos botões CTA */
    } as const;
    ```

    No bloco SITE_TITLE e SITE_DESCRIPTION:
    ```typescript
    /* EDITÁVEL: título da aba do navegador e meta title para SEO */
    export const SITE_TITLE = "...";

    /* EDITÁVEL: meta description (~150 chars) exibida nos resultados de busca */
    export const SITE_DESCRIPTION = "...";
    ```

    No bloco CTA_PRIMARY:
    ```typescript
    /* EDITÁVEL: texto do botão CTA principal — aparece em Navbar, Hero e Footer */
    export const CTA_PRIMARY = "Falar com um consultor";
    ```

    No bloco CTA_FORM:
    ```typescript
    /* EDITÁVEL: texto do botão de submit no formulário de contato */
    export const CTA_FORM = "Agendar conversa com consultor";
    ```

    No bloco FORM_SUCCESS:
    ```typescript
    /* EDITÁVEL: mensagem exibida após envio do formulário */
    export const FORM_SUCCESS = "...";
    ```

    No bloco DIAGNOSTICO (cards):
    ```typescript
    // Dentro do array cards, antes de cada objeto:
    /* EDITÁVEL: título e body do card de diagnóstico */
    { title: "Visibilidade tardia", body: "...", ... },
    ```
    — Adicione o comentário uma vez antes do array `.cards`, não em cada item:
    ```typescript
    /* EDITÁVEL: os 3 cards de diagnóstico — títulos e bodys substituíveis */
    cards: [...]
    ```

    No bloco PROVA_SOCIAL (métricas e depoimento):
    ```typescript
    /* EDITÁVEL: substitua com métricas reais validadas pelo cliente antes do go-live */
    metrics: [...],
    /* EDITÁVEL: substitua com depoimento real autorizado pelo cliente antes do go-live */
    depoimento: {...},
    /* EDITÁVEL: substitua com logotipos reais de clientes */
    logos: [...],
    ```

    No bloco FOOTER (copyright e contactEmail):
    ```typescript
    /* EDITÁVEL: ano e nome jurídico da empresa */
    copyright: "© 2026 Pruma Consultoria...",
    ```
    ```typescript
    /* EDITÁVEL: defina NEXT_PUBLIC_CONTACT_EMAIL no .env.local — ver STATE.md Pending Todos */
    const contactEmail = process.env.NEXT_PUBLIC_CONTACT_EMAIL ?? "contato@pruma.com.br";
    ```

    Os demais blocos (TESE, COMO_FUNCIONA, PAINEL_GESTOR, FUNCIONALIDADES, PARA_QUEM,
    DIFERENCIAL, CTA_FINAL) devem receber apenas um comentário de seção no topo do bloco:
    ```typescript
    /* EDITÁVEL: copy da seção [NOME] */
    export const NOME_BLOCO = { ... } as const;
    ```

    PARTE C: VERIFICAÇÃO DOS 3 TOUCHPOINTS CTA_PRIMARY

    Confirme que `CTA_PRIMARY` é consumido em exatamente estes 3 arquivos:
    1. components/Navbar.tsx — botão "Falar com um consultor" no header sticky
    2. components/Hero.tsx — botão primário na seção hero
    3. components/Footer.tsx — botão ghost na coluna de brand do footer

    Execute:
    ```bash
    grep -rn "CTA_PRIMARY" /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/components/ --include="*.tsx"
    ```

    Se Navbar.tsx, Hero.tsx e Footer.tsx NÃO aparecerem no resultado, adicione o import
    e uso de `CTA_PRIMARY` nesses arquivos. Se Footer.tsx não tiver CTA_PRIMARY no brand
    column, adicione um Button variant="ghost" href="#contato" com {CTA_PRIMARY} no bloco
    da coluna de brand, depois do `<p className="...tagline...">`.

    Atenção: A contagem de touchpoints inclui apenas componentes com renderização direta
    para o usuário. CTAFinal.tsx usa `CTA_FORM` (texto diferente) — não conta como touchpoint
    do CTA_PRIMARY.
  </action>

  <verify>
    <automated>
      cd /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas && grep -c "EDITÁVEL" lib/constants.ts && grep -l "CTA_PRIMARY" components/Navbar.tsx components/Hero.tsx components/Footer.tsx | wc -l | grep -q "3" && echo "CTA_PRIMARY: 3 touchpoints OK" && grep -rn "CTA_PRIMARY" components/ --include="*.tsx" | wc -l
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "EDITÁVEL" lib/constants.ts` retorna número >= 12 (pelo menos 12 ocorrências do marcador)
    - `grep -l "CTA_PRIMARY" components/Navbar.tsx components/Hero.tsx components/Footer.tsx` retorna exatamente 3 arquivos
    - `grep -rn "CTA_PRIMARY" components/ --include="*.tsx"` mostra matches em Navbar.tsx, Hero.tsx e Footer.tsx
    - `lib/constants.ts` contém a string `/* EDITÁVEL:` em pelos menos 12 locais distintos
    - `grep -rn "\"Falar com um consultor\"" components/ --include="*.tsx"` retorna ZERO resultados (string não está hardcoded, vem de CTA_PRIMARY)
    - `grep -rn "\"Agendar conversa" components/ --include="*.tsx"` retorna ZERO resultados (vem de CTA_FORM)
  </acceptance_criteria>

  <done>
    lib/constants.ts tem comentários EDITÁVEL em todos os campos de copy substituível. CTA_PRIMARY
    é referenciado em Navbar.tsx, Hero.tsx e Footer.tsx (3 touchpoints distintos). Nenhuma string
    de copy visível está hardcoded em componentes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lib/constants.ts → componentes | Strings de copy fluem de fonte única para renderização |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | lib/constants.ts | accept | Arquivo é código-fonte; alterações são rastreadas por git. LP não aceita input de usuário nesse fluxo. |
| T-04-04 | Information Disclosure | NEXT_PUBLIC_CONTACT_EMAIL | mitigate | Variável já usa prefixo NEXT_PUBLIC_ (intencional para client-side). Email de contato é informação pública. |
</threat_model>

<verification>
```bash
# Verificar comentários EDITÁVEL em constants.ts
grep -c "EDITÁVEL" /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/lib/constants.ts
# Deve retornar >= 12

# Verificar 3 touchpoints do CTA
grep -rn "CTA_PRIMARY" /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/components/ --include="*.tsx"
# Deve mostrar matches em: Navbar.tsx, Hero.tsx, Footer.tsx

# Garantir que não há copy hardcoded relevante
grep -rn '"Falar com um consultor"' /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/components/ --include="*.tsx"
# Deve retornar 0 resultados
```
</verification>

<success_criteria>
- lib/constants.ts contém `/* EDITÁVEL:` em >= 12 locais
- CTA_PRIMARY referenciado em Navbar.tsx, Hero.tsx e Footer.tsx
- Zero strings de copy visível hardcoded em componentes (confirmado por grep)
- `npm run build` ainda passa (sem regressão)
</success_criteria>

<output>
Após conclusão, crie `.planning/phases/04-quality-polish/04-02-SUMMARY.md`
</output>
