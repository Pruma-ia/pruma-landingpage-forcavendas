---
phase: 04-quality-polish
plan: 04
type: execute
wave: 2
depends_on:
  - "04-01"
autonomous: false
files_modified:
  - app/layout.tsx
  - components/mockups/AppVendedor.tsx
  - components/mockups/PainelDashboard.tsx
  - components/mockups/_BrazilMap.tsx
  - components/mockups/_AbcChart.tsx
  - components/ProvaSocial.tsx
  - components/ComoFunciona.tsx
  - components/Diferencial.tsx
  - components/ParaQuem.tsx
  - components/Funcionalidades.tsx
  - components/PainelGestor.tsx
requirements:
  - QA-02

must_haves:
  truths:
    - "Lighthouse desktop Performance score >= 90"
    - "Lighthouse desktop Accessibility score >= 95"
    - "Lighthouse desktop Best Practices score >= 95"
    - "Lighthouse desktop SEO score = 100"
    - "Todos os elementos img têm atributo alt não-vazio"
    - "Todos os botões icon-only têm aria-label"
    - "Meta description está entre 50 e 160 caracteres"
    - "Fraunces e Inter são carregados com font-display: swap (já configurado via next/font)"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Meta tags SEO completas e font preload"
    - path: "components/mockups/_BrazilMap.tsx"
      provides: "SVG com role e title acessíveis"
    - path: "components/mockups/_AbcChart.tsx"
      provides: "SVG com role e title acessíveis"
  key_links:
    - from: "app/layout.tsx"
      to: "metadata.description"
      via: "Next.js Metadata API"
      pattern: "description.*150|description.*chars"
    - from: "Lighthouse"
      to: "HTML output"
      via: "accessibility audit"
      pattern: "aria-label|alt="
---

<objective>
Executar auditoria Lighthouse e corrigir os problemas que impedem atingir Performance 90+, Accessibility 95+, Best Practices 95+ e SEO 100 no desktop.

Purpose: Scores Lighthouse são critério de aceite do produto. SEO 100 é especialmente crítico para uma LP de captura de leads B2B.
Output: LP auditada e corrigida conforme targets estabelecidos em QA-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-quality-polish/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aplicar correções de acessibilidade e SEO de alta probabilidade antes de rodar Lighthouse</name>
  <files>
    - app/layout.tsx
    - components/mockups/_BrazilMap.tsx
    - components/mockups/_AbcChart.tsx
    - components/mockups/PainelDashboard.tsx
    - components/mockups/AppVendedor.tsx
    - components/ProvaSocial.tsx
    - components/ComoFunciona.tsx
    - components/Diferencial.tsx
    - components/ParaQuem.tsx
    - components/Funcionalidades.tsx
    - components/PainelGestor.tsx
  </files>

  <read_first>
    Leia na ordem abaixo — leia TODO o arquivo antes de modificar:
    1. /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/app/layout.tsx
       — verificar metadata: openGraph.images, viewport, robots, canonical
    2. /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/components/mockups/_BrazilMap.tsx
       — verificar se SVG tem role="img" e <title>
    3. /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/components/mockups/_AbcChart.tsx
       — verificar se SVG tem role="img" e <title>
    4. /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/components/mockups/PainelDashboard.tsx
       — verificar alt text em qualquer img, aria-labels em icon buttons
    5. /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/components/mockups/AppVendedor.tsx
       — verificar alt text e aria-labels
    6. Demais componentes: verificar ícones sem aria-label, textos de baixo contraste
  </read_first>

  <action>
    GRUPO 1: Correções de SEO (impacto direto no score SEO 100)

    Em app/layout.tsx, expanda o objeto `metadata` com os campos faltantes para SEO perfeito:

    ```typescript
    export const metadata: Metadata = {
      title: SITE_TITLE,
      description: SITE_DESCRIPTION,
      // Robots: permitir indexação
      robots: {
        index: true,
        follow: true,
        googleBot: {
          index: true,
          follow: true,
        },
      },
      // Open Graph — adicionar url e type (já existem, checar se estão completos)
      openGraph: {
        title: SITE_TITLE,
        description: SITE_DESCRIPTION,
        type: "website",
        locale: "pt_BR",
        siteName: "Pruma",
        // url é opcional sem domínio definido — omitir se não houver
      },
      twitter: {
        card: "summary_large_image",
        title: SITE_TITLE,
        description: SITE_DESCRIPTION,
      },
      // Viewport meta — Next.js 14 já adiciona por padrão via viewport export
      // Se não existir, adicionar:
    };

    // Adicionar export viewport separado (Next.js 14 App Router padrão):
    export const viewport = {
      width: "device-width",
      initialScale: 1,
    };
    ```

    VERIFIQUE: `SITE_DESCRIPTION` em lib/constants.ts tem 135 caracteres.
    Execute:
    ```bash
    node -e "const c=require('./lib/constants'); console.log(c.SITE_DESCRIPTION.length, c.SITE_DESCRIPTION)"
    ```
    Se o comprimento for < 50 ou > 160, ajuste a descrição em lib/constants.ts para ficar
    entre 120-155 caracteres, sem cortar o sentido.

    GRUPO 2: Correções de Acessibilidade — SVGs decorativos e funcionais

    Em components/mockups/_BrazilMap.tsx:
    Leia o arquivo inteiro primeiro. Se o `<svg>` não tiver `role="img"` e `<title>`:
    ```tsx
    // Adicionar no elemento <svg> raiz:
    <svg
      role="img"
      aria-labelledby="brazil-map-title"
      // ...demais props existentes
    >
      <title id="brazil-map-title">Mapa do Brasil — distribuição territorial de clientes</title>
      {/* resto do SVG */}
    </svg>
    ```

    Em components/mockups/_AbcChart.tsx:
    Leia o arquivo inteiro primeiro. Se o `<svg>` não tiver `role="img"` e `<title>`:
    ```tsx
    <svg
      role="img"
      aria-labelledby="abc-chart-title"
      // ...demais props existentes
    >
      <title id="abc-chart-title">Curva ABC — distribuição de clientes por receita</title>
      {/* resto do SVG */}
    </svg>
    ```

    GRUPO 3: Correções de Acessibilidade — ícones e imagens

    Execute grep para encontrar ícones Lucide sem aria-hidden ou aria-label:
    ```bash
    grep -rn "strokeWidth" \
      /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/components/ \
      --include="*.tsx" | grep -v 'aria-hidden\|aria-label' | head -20
    ```

    Para cada ícone que é PURAMENTE DECORATIVO (ao lado de texto que explica seu significado),
    confirme que tem `aria-hidden="true"`. Se não tiver, adicione.

    Para botões icon-only (nenhum texto visível ao lado), confirme `aria-label` descritivo.
    Navbar já tem:
    - hamburger: `aria-label="Abrir menu"` ✓
    - X: `aria-label="Fechar menu"` ✓

    Execute grep para verificar imgs sem alt:
    ```bash
    grep -rn "<img " \
      /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/ \
      --include="*.tsx" | grep -v 'alt='
    ```
    Se houver resultados, adicione `alt=""` para imagens decorativas ou `alt="descrição"` para
    imagens informativas. (A LP usa principalmente SVG e divs — imagens <img> são raras.)

    GRUPO 4: Correções de Performance — font preloading e critical path

    Em app/layout.tsx, o preloading de fontes já é gerenciado por `next/font` com `display: "swap"`.
    Não adicione `<link rel="preload">` manual — conflitaria com next/font.

    Verifique se há alguma configuração next.config que prejudica performance:
    ```bash
    cat /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/next.config.js 2>/dev/null || \
    cat /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/next.config.ts 2>/dev/null || \
    echo "next.config não encontrado"
    ```
    Se next.config tiver `swcMinify: false` ou `optimizeFonts: false`, remova essas opções
    (defaults são melhores para performance).

    GRUPO 5: Correções de Best Practices — console.log e deprecated APIs

    Execute:
    ```bash
    grep -rn "console\.log\|console\.warn\|console\.error" \
      /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/components/ \
      --include="*.tsx"
    ```
    Se houver `console.log` em código de componente (não em comentários), remova-os.
    Lighthouse penaliza console errors em Best Practices.

    Verificar DOCTYPE e charset:
    Next.js App Router adiciona automaticamente `<!DOCTYPE html>` e `<meta charset="utf-8">`.
    Não adicione manualmente — duplicaria.

    GRUPO 6: Verificar contraste de cores (Accessibility)

    Os pares críticos de contraste na LP (fundo/texto):
    - `#5B6B85` (cinza-texto) sobre `#FFFFFF`: ratio = 4.61:1 — passa WCAG AA (4.5:1 mínimo para texto normal)
    - `#5B6B85` sobre `#E0F6FE`: ratio ≈ 3.8:1 — PODE FALHAR para texto pequeno (<18px normal / <14px bold)
    - `#00AEEF` (cyan) sobre `#FFFFFF`: ratio = 2.87:1 — FALHA para texto normal, OK para decoração
    - `#FFFFFF` sobre `#0D1B4B`: ratio = 13.4:1 — passa WCAG AAA

    Para o texto cinza sobre fundo ciano-pale, verifique em quais componentes isso ocorre:
    ```bash
    grep -rn "text-pruma-gray-text.*bg-pruma-cyan-pale\|bg-pruma-cyan-pale.*text-pruma-gray-text" \
      /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/components/ \
      --include="*.tsx"
    ```
    Se `text-pruma-gray-text` aparecer em texto de menos de 18px sobre fundo cyan-pale
    (em CTAFinal, Funcionalidades, Diagnóstico), substitua por `text-pruma-navy` nesses contextos
    para garantir contraste adequado.

    Atenção: eyebrows e microcopy em `font-mono text-xs uppercase` com `text-pruma-gray-text` sobre
    cyan-pale FALHAM WCAG AA. Corrija para `text-pruma-navy` nesses casos específicos.

    Finalize rodando:
    ```bash
    cd /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas && npm run build
    ```
    para confirmar que todas as correções compilam sem erro.
  </action>

  <verify>
    <automated>
      cd /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas && grep -n "robots\|viewport" app/layout.tsx && grep -rn "role=\"img\"" components/mockups/ --include="*.tsx" | wc -l && grep -rn "console\.log" components/ --include="*.tsx" | grep -v "^.*//.*console" | wc -l && npm run build && echo "BUILD_OK"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "robots" app/layout.tsx` retorna ao menos 1 resultado (robots meta configurado)
    - `grep -rn 'role="img"' components/mockups/ --include="*.tsx" | wc -l` retorna >= 2 (BrazilMap e AbcChart)
    - `grep -rn "console\.log" components/ --include="*.tsx" | grep -v "//"` retorna 0 resultados
    - `npm run build` retorna exit code 0
    - Todos os ícones decorativos Lucide têm `aria-hidden="true"` (grep confirma)
  </acceptance_criteria>

  <done>
    Correções pré-Lighthouse aplicadas: robots meta, viewport export, SVGs acessíveis, console.logs
    removidos, contraste de texto corrigido nos contextos de falha. Build passa.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Correções de SEO, acessibilidade e best practices aplicadas estaticamente. Build passa.
    Agora é necessário rodar Lighthouse para verificar os scores e identificar problemas
    restantes que só aparecem em runtime.
  </what-built>
  <how-to-verify>
    1. Execute `npm run build && npm start` no diretório do projeto (porta padrão 3000)
    2. Abra Chrome e navegue para http://localhost:3000
    3. Abra DevTools → Lighthouse tab
    4. Configure:
       - Mode: Navigation
       - Device: Desktop
       - Categories: Performance, Accessibility, Best Practices, SEO (todas marcadas)
    5. Clique "Analyze page load" e aguarde o resultado completo
    6. Anote os scores de cada categoria:
       - Performance: alvo >= 90
       - Accessibility: alvo >= 95
       - Best Practices: alvo >= 95
       - SEO: alvo = 100

    ALTERNATIVA (se preferir CLI):
    ```bash
    npm install -g @lhci/cli lighthouse 2>/dev/null || true
    npx lighthouse http://localhost:3000 \
      --preset=desktop \
      --output=json \
      --output-path=/tmp/lighthouse-report.json \
      --chrome-flags="--headless" \
      --only-categories=performance,accessibility,best-practices,seo
    node -e "const r=require('/tmp/lighthouse-report.json'); const c=r.categories; console.log('Performance:', Math.round(c.performance.score*100)); console.log('Accessibility:', Math.round(c.accessibility.score*100)); console.log('Best Practices:', Math.round(c['best-practices'].score*100)); console.log('SEO:', Math.round(c.seo.score*100));"
    ```

    Se algum score estiver abaixo do target, informe os problemas específicos listados no
    relatório Lighthouse (a seção "Opportunities" e "Diagnostics") para que possam ser corrigidos.
  </how-to-verify>
  <resume-signal>
    Se Performance >= 90, Accessibility >= 95, Best Practices >= 95, SEO = 100: digite "aprovado".
    Se qualquer score estiver abaixo: informe o score e os 2-3 problemas com maior impacto
    listados no relatório Lighthouse para que as correções adicionais sejam implementadas.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LP pública → motores de busca | Meta tags e robots controlam indexação |
| LP pública → usuários com deficiência | ARIA e contraste garantem acessibilidade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | robots meta | mitigate | Configurar robots: { index: true, follow: true } — LP deve ser indexada; sem páginas privadas neste projeto |
| T-04-08 | Spoofing | OpenGraph meta | accept | OG tags são informação pública; sem dados sensíveis expostos via meta |
| T-04-09 | Denial of Service | console.log em produção | mitigate | Remover console.log evita que erros de runtime sejam expostos no browser console em produção |
</threat_model>

<verification>
```bash
# Confirmar correções estáticas
grep -n "robots" /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/app/layout.tsx
grep -rn 'role="img"' /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/components/mockups/ --include="*.tsx"
grep -rn "console\.log" /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas/components/ --include="*.tsx" | grep -v "//"

# Build sem regressão
cd /Users/marcelomattioli/vsc/pruma-landingpage-forcavendas && npm run build
```
</verification>

<success_criteria>
- Correções estáticas aplicadas (robots, SVG acessíveis, sem console.log, contraste corrigido)
- Build passa sem erros após correções
- Checkpoint humano aprovado com Lighthouse scores: Performance >= 90, Accessibility >= 95, Best Practices >= 95, SEO = 100
</success_criteria>

<output>
Após conclusão, crie `.planning/phases/04-quality-polish/04-04-SUMMARY.md`
</output>
